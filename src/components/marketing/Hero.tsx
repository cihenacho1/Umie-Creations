import { RoseScrollHero } from "@/components/RoseScrollHero";

/** Full-screen scroll-synced cinematic hero — Apple-style choreography + pinned video background. */
export function Hero() {
  return <RoseScrollHero />;
}
